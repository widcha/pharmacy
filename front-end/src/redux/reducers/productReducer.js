const INITIAL_STATE = {
	product_list: [],
	loading: false,
	error: "",
};

export const productReducer = (state = INITIAL_STATE, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
