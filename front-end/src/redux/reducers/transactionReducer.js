const INITIAL_STATE = {
    transaction_list: [],
    loading: false,
    error: '',
};

export const transactionReducer = ( state = INITIAL_STATE, action ) => {
    switch (action.type) {
        default:
            return state
    }
};