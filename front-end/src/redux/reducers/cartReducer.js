const INITIAL_STATE = {
  cart_list: [],
  loading: false,
  error: "",
};

export const cartReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
